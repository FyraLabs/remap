name: Rust

on:
  push:
  pull_request:

jobs:
  rust:
    uses: FyraLabs/actions/.github/workflows/rust.yml@main
    with:
      test-container: '{"image": "ghcr.io/terrapkg/builder:f40"}'
      test-pre: |
        dnf5 install -y libhelium-devel gcc
